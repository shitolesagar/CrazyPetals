@model CrazyPetals.Entities.WebViewModels.AddSubcategoryViewModel
@{
    ViewData["Title"] = "Add Subcategory";
    string stringHtml = "<span class=\"display-inline\">Add Subcategories</span>";
    ViewData["TabHeading"] = stringHtml;
    ViewData["ActiveTab"] = "subcategory";
}
<div class="row">
    <div class="col-md-6 detail-container">
        <form asp-controller="subcategory" asp-action="add" autocomplete="off" spellcheck="false">
            <div class="detail-container">
                <div class="row">
                    <div class="col-md-9">
                        <div class="form-group">
                            <label asp-for="SubcategoryName" class="control-label float-label">Subcategory Name <span class="f-red">*</span></label>
                            <input type="text" asp-for="SubcategoryName" class="form-control" maxlength="40" value="" />
                            <span asp-validation-for="SubcategoryName" class="text-danger mb-3"></span>
                        </div>
                    </div>
                </div>
                <div class="row mb-4">
                    <div class="col-md-9">
                        <div class="form-group">
                            <label class="float-label">Select Category</label>
                            <div class="my-custom-select width-100">
                                <select class="form-control select-dropDown" asp-for="CategoryId" value="" asp-items="ViewBag.CategoriesList">
                                    <option value="">select</option>
                                </select>
                            </div>
                            <span asp-validation-for="CategoryId" class="text-danger"></span>
                        </div>
                    </div>
                </div>
                <div class="detailBtn-group">
                    <button type="submit" class="btn btn-primary">Add</button>
                    <a asp-controller="subcategory" asp-action="index" class="btn btn-secondary">Cancel</a>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
